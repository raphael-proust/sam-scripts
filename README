A collection of sam scripts.

These are more for learning purpose than actual use. (It is still fully
functional.)

Usage: `sam -d file [other files] <script.sam`

Requirement: `sam` (checkout [9base.suckless.org])

